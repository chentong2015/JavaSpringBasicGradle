// Declare the configuration required to configure the hierarchy of projects which participate in a build
// Include root project, multiple sub projects ...
rootProject.name = 'JavaSpringBasicGradle'
include 'web'
include 'core'

